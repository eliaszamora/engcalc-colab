name: EngCalc 0.9.0 Task 6 Context

on:
  push:
    branches:
      - feature/v0.9.0-matrix-cas
    paths:
      - .github/workflows/v090-task6-context.yml

permissions:
  contents: write

jobs:
  context:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/v0.9.0-matrix-cas
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Patch CURRENT
        run: python .github/scripts/v090_task6_context.py
      - name: Validate context patch
        run: |
          git diff --check
          grep -q "TASKS 0–6 COMPLETE, TASK 7 NEXT" docs/project-context/CURRENT.md
          grep -q "665/665 full GREEN" docs/project-context/CURRENT.md
          grep -q "67b22d531955e8795e446afefc0ad0e698c9973d" docs/project-context/CURRENT.md
      - name: Commit context
        run: |
          git config user.name "eliaszamora"
          git config user.email "37598447+eliaszamora@users.noreply.github.com"
          git add docs/project-context/CURRENT.md
          git commit -m "docs: close Matrix CAS Task 6"
          git push origin HEAD:feature/v0.9.0-matrix-cas
