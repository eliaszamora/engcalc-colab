name: EngCalc 0.6.1 compact label validation

on:
  push:
    branches:
      - feature/v0.6.1-visual-polish-2

jobs:
  compact-labels:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install
        run: python -m pip install -e '.[dev]' ipython
      - name: Compact labels focused suite
        run: pytest -q tests/test_compact_characteristic_labels_v061.py
