name: Apply EngCalc 0.7.2 Task 4

on:
  push:
    branches:
      - feature/v0.7.2-engineering-tables
    paths:
      - tools/apply_v072_task4.py
      - .github/workflows/v072-task4-apply.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/v0.7.2-engineering-tables
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Apply patch
        run: python tools/apply_v072_task4.py
      - name: Commit product change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/engcalc_colab/renderer.py src/engcalc_colab/magic.py
          git diff --cached --quiet && exit 0
          git commit -m "feat: render engineering tables in eng magic"
          git push origin HEAD:feature/v0.7.2-engineering-tables
