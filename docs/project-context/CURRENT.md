# EngCalc Current Project Context

_Last updated: 2026-08-30 — EngCalc 0.9.0 Matrix/CAS remains canonical on `main`. EngCalc 0.9.1 exact characteristics is being implemented inline on `feature/v0.9.1-exact-characteristics`. Tasks 1–10 are complete and fully regression-tested. All temporary Task 10 validation infrastructure has been removed. The exact next step is Task 11 end-to-end acceptance, README examples and complete pre-release regression._

## Current baseline

- Repository: `eliaszamora/engcalc-colab`.
- Canonical integrated release: **EngCalc 0.9.0 Matrix/CAS**.
- Canonical 0.9.1 base: **`main@cdc454db7ea43e57e334d523afded8b4ef498ded`**.
- Runtime/package version remains **0.9.0** until release-closing Task 12.
- Active branch: **`feature/v0.9.1-exact-characteristics`**.
- Approved design spec: `docs/superpowers/specs/2026-08-30-engcalc-v0.9.1-exact-characteristics-design.md`.
- Implementation plan: `docs/superpowers/plans/2026-08-30-engcalc-v0.9.1-exact-characteristics-implementation.md`.
- User selected **inline execution / executing-plans**.
- Task 7 product: **`44401cd2ab3d89d10b861a45f873342756af8d41`**.
- Task 8 product: **`0fe830723d12c4899e1e37cf8f3ec441cad708fe`**.
- Task 9 product: **`c9055a10fdeb2bae7d3d86f0740597567f142fcf`**.
- Task 10 product: **`3395bf304a5560c34d6ef3b0053ad8c2810ac418`**.
- Task 10 cleanup head before this context update: **`1698c4d852c956d02494768cb332dbf97fdc64c7`**.
- Never invoke Codex / `@codex review` / Codex Cloud without explicit user authorization.
- Never merge 0.9.1 into `main` without explicit user approval.

## Approved behavior

### Existing released behavior preserved

- `%%eng` remains a restricted EngCalc DSL; ordinary notebook cells remain Python.
- Numeric/Pint semantics, Piecewise, tables, plots, envelopes, multi-argument functions and Matrix/CAS behavior remain regression requirements.
- Whole matrices remain invalid scalar characteristic/plot/table/envelope responses; indexed scalar matrix expressions remain supported.
- Positive structural moment remains plotted **downward**.
- Ordinary plot curves retain the 201-point sampling policy; the plot grid is never an authoritative characteristic solver.

### Approved 0.9.1 characteristic contract

Public standalone calls:

```text
extrema(response, variable, lower, upper)
roots(response, variable, lower, upper)
intersections(response_1, response_2, variable, lower, upper)
```

- Characteristic calls are standalone-only in 0.9.1; assignment, nesting and composition are rejected.
- One reusable exact-first core lives in `src/engcalc_colab/characteristics.py`.
- Domains are finite closed real intervals with unit-compatible bounds and `lower < upper`.
- Exact symbolic results are authoritative whenever usable; deterministic numeric fallback runs only for unresolved continuous regions.
- Fallback uses 1025 internal physical-domain samples, residual tolerance 1e-9 and x de-duplication tolerance 1e-10; it never uses the public plot grid and adds no SciPy dependency.
- Roots, intersections and extrema preserve Piecewise topology, units, exact/numeric provenance, interval loci and explicit discontinuity semantics.
- Engine dispatch returns typed immutable `RootsResult`, `IntersectionsResult` and `ExtremaResult`, with operation-specific line-numbered diagnostics.
- Standalone characteristic rendering uses dedicated HTML/MathJax: exact points show `=`, numeric fallback shows `≈`, units/roles/sides/loci/unbounded behavior are explicit.
- `%%eng` flushes pending aligned equations before characteristic blocks and preserves source order.
- `PlotSeries` now carries immutable `characteristics: tuple[CharacteristicPoint, ...]` metadata.
- For ordinary `plot(...)`, exact global extrema are generated by the characteristic core independently of the 201 sampled drawing grid and then consumed by presentation code. Multi-series and parameter sweeps carry independent exact metadata; sweep values are passed via overrides.
- Plot presentation preserves the historical maximum one max / one min callout per series even when the mathematical core contains several equivalent global points; mathematical metadata remains complete.
- Exact characteristic enrichment is non-breaking presentation metadata: if the exact core cannot safely partition a plot-valid `abs(...)`/symbolic Piecewise form, the ordinary plot remains valid and presentation falls back to the historical sampled callout path rather than failing the plot.
- Constant global-extremum intervals are not expanded into repeated plot markers.
- `envelope(...)` deliberately remains on its sampled characteristic path in 0.9.1; exact envelope crossovers/governing intervals remain deferred to **0.9.2**.

## Open issues / user feedback

- No blocking 0.9.1 design ambiguity remains.
- **Tasks 1–10 are complete. Task 11 is next.**
- `no_vertical_scroll()` remains a separate ergonomics issue.
- Multiline ordinary non-matrix function-call parsing remains separate.
- Generalized structural eigenproblems remain deferred.

## Validation evidence

### Canonical 0.9.0 release

- Release Actions **`33332233490`**, job **`99312713507`**.
- Release contract **23/23 GREEN**.
- Source suite before wheel **721/721 GREEN in 142.99 s**.
- Wheel `engcalc_colab-0.9.0-py3-none-any.whl`; SHA-256 **`ea66fa231b5657695e2c38cefb324da220070a2f7c86557dddef19d2017a0719`**.
- Installed-wheel/source-free suite **721/721 GREEN in 141.38 s**.
- Final source revalidation **721/721 GREEN in 139.76 s**.

### 0.9.1 Tasks 1–9

- Task 1 parser/API + typed models: **758/758 full GREEN**.
- Task 2 exact continuous roots: **772/772 full GREEN**.
- Task 3 Piecewise roots/topology: **783/783 full GREEN**.
- Task 4 intersections: **794/794 full GREEN**.
- Task 5 continuous extrema: **800/800 full GREEN**.
- Task 6 Piecewise extrema: **808/808 full GREEN**.
- Task 7 deterministic fallback: **820/820 full GREEN**.
- Task 8 engine dispatch + diagnostics: product **`0fe830723d12c4899e1e37cf8f3ec441cad708fe`**; authoritative run **`33340983117`**, job **`99336452342`** — **368/368 broad**, **828/828 full in 104.51 s**.
- Task 9 rendering + `%%eng` routing: product **`c9055a10fdeb2bae7d3d86f0740597567f142fcf`**; authoritative run **`33341538689`**, job **`99337950723`** — **387/387 broad in 49.23 s**, **835/835 full in 117.02 s**.

### 0.9.1 Task 10 — authoritative exact characteristics in ordinary plots

- Decisive RED fixture uses `f(x) = -(x - 1/3)^2 + 2` on `[0,1]`; `1/3` is not a node of the 201-point uniform plot grid, while the authoritative characteristic must be exactly `1/3`.
- Initial RED run **`33341860971`**, job **`99338819346`**: **8 failed / 52 passed in 41.95 s**. Failures were the intentionally absent `PlotSeries.characteristics` contract and the sampled maximum at **0.335** instead of **1/3**; historical selected plot/presentation regressions remained GREEN.
- During GREEN integration, the first attempt exposed three real compatibility issues before any product commit: exact enrichment could break previously valid `abs(...)`/symbolic Piecewise plots; equal global endpoint extrema produced duplicate callouts; and one RED assertion incorrectly assumed Piecewise breakpoints could not be injected into the adaptive sample grid.
- The compatibility correction keeps exact mathematics authoritative where resolved, makes exact plot metadata optional enrichment for unsupported symbolic topology, preserves one max/one min presentation callout, and tests the Piecewise symbolic point/request coordinate rather than sample-grid absence.
- Corrected focused GREEN run **`33342464291`**, job **`99340455274`**: **61/61 PASS in 46.67 s**; product committed as **`3395bf304a5560c34d6ef3b0053ad8c2810ac418`** (`feat: use exact characteristics in ordinary plots`).
- Idempotent recheck run **`33342576393`**, job **`99340758847`**: **61/61 PASS in 33.62 s**; log explicitly states `Task 10 exact plot characteristics already present; skipping base patch.` and `No product patch to commit.`
- Authoritative full-gate SHA: **`2844da00092e22b070c42ec8da6e90eb3ce24467`**.
- Full-gate run **`33342633493`**, job **`99340916954`**, Python **3.13.15**.
- `compileall`: PASS.
- `git diff --check origin/main...HEAD`: PASS.
- Exact-characteristic plot + presentation + Piecewise regression gate: **235/235 PASS in 62.91 s**.
- Complete source suite: **844/844 PASS in 142.78 s**.
- Four temporary Task 10 artifacts were removed after validation: `.github/workflows/v091-task10-tdd.yml`, `.github/workflows/v091-task10-full-gate.yml`, `.github/scripts/v091_task10_green.py`, `.github/scripts/v091_task10_green_fix.py`.
- Cleanup head before this context update: **`1698c4d852c956d02494768cb332dbf97fdc64c7`**.
- No Codex review or Codex Cloud has been invoked.

## Roadmap / active plan

- **0.9.0 Matrix/CAS:** COMPLETE + RELEASE-VALIDATED + MERGED.
- **0.9.1 Tasks 1–10:** **COMPLETE — latest full suite 844/844 GREEN**.
- **0.9.1 Task 11 acceptance/docs/complete pre-release regression: NEXT**.
- **0.9.1 Task 12 release/version/wheel/source-free validation/PR: AFTER Task 11**.
- Later: **0.9.2 exact envelopes/governing intervals → 0.9.3 named response cases/combinations → 0.10.x engineering verification → 1.0.0 stabilization**.

## Exact next step

1. Create `tests/test_characteristics_acceptance.py` and exercise one persistent `EngineeringEngine` state containing dimensional beam moment/shear responses, exact extrema, roots, intersections and an indexed matrix-scalar root.
2. Add acceptance coverage for a Piecewise sign jump that is not a root and a deterministic fallback point with `provenance="numeric"`.
3. Assert typed results, exact symbolic locations, physical units and indexed-matrix scalar support.
4. Run the focused characteristic acceptance set before documentation changes.
5. Add README executable examples in exactly six categories: beam-like moment extrema; shear roots; response intersections; Piecewise jump/no-false-root; indexed matrix scalar analysis; approximate fallback marked with `≈`.
6. State explicitly that characteristic calls are standalone in 0.9.1 and exact envelope governing intervals are deferred to 0.9.2.
7. Run `compileall`, the complete source suite and diff hygiene. Record actual counts/times; remove any temporary Task 11 validation harness before closure.
8. Keep version at 0.9.0 until Task 12. Do not merge without explicit user approval and do not invoke Codex unless explicitly authorized.

## How to resume in a new conversation

Read this file first. EngCalc 0.9.0 remains canonical on `main@cdc454db7ea43e57e334d523afded8b4ef498ded`. Active work is `feature/v0.9.1-exact-characteristics`. Tasks 1–10 are complete. Task 10 product is `3395bf304a5560c34d6ef3b0053ad8c2810ac418`; authoritative run `33342633493`, job `99340916954`, with **235/235 broad exact-plot/presentation tests** and **844/844 complete source tests in 142.78 s**. All Task 10 temporary harness files are removed; cleanup head before this context update is `1698c4d852c956d02494768cb332dbf97fdc64c7`. Resume with Task 11 acceptance tests and README examples. Never merge without explicit user approval; never invoke Codex without explicit authorization.
